@{
    Layout = null;
}

<!DOCTYPE html>

<html>
    <head>
        @Html.Partial("ContentHelper")
        <title>MS</title>
        <script>
            var musGround = {};
            $(document).ready(function() {
                musGround = getMusGround({
                    logUrl: '@Url.Action("LogAction", "Log")'
                });
            });
        </script>
    </head>
    <body>
        <div class="all-space">
            @Html.Partial("Header")
            <div class="content">
                @RenderBody()
            </div>
            @Html.Partial("Footer")
        </div>
    </body>
</html>